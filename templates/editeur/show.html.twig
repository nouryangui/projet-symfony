{% extends 'base.html.twig' %}

{% block title %}{{titre}}{% endblock %}

{% block body %}

    <table class="table" border="1">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editeur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ editeur.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ editeur.adresse }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ editeur.pays }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ editeur.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('editeur_index') }}">Retour</a>

    <a href="{{ path('editeur_edit', {'id': editeur.id}) }}">editer</a>

    {{ include('editeur/_delete_form.html.twig') }}
{% endblock %}
